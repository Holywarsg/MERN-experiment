{
    "buildCommand": "cd client && npm install && npm run build",
    "outputDirectory": "client/dist",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/api/(.*)", //источник всех API
            "destination": "/api/index.js" // Выход всех API
        }
    ]
}